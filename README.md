# CCAF (Vue Client)

Vue 3 SPA — клиент для каталога юнитов BattleTech Alpha Strike. Мобильно-ориентированный интерфейс в стиле военного терминала: тёмная тема, зелёные акценты, scanlines.

## ⚠️ Дисклеймер

**BattleTech** — зарегистрированная торговая марка [Catalyst Game Labs](https://www.catalystgamelabs.com/) / Topps.  
Все данные юнитов, названия и игровые механики являются собственностью соответствующих правообладателей.

Этот проект является **неофициальной фанатской разработкой**, созданной исключительно в образовательных целях.  
Проект не аффилирован с Catalyst Game Labs, не одобрен и не спонсируется ею.

Исходный код приложения распространяется под лицензией MIT (см. [LICENSE](LICENSE)).

## Описание

**CCAF** (Capellan Confederation Armed Forces)/**ВС КК** (Вооружённые Силы Капелланской Конфедерации) — клиентская часть приложения для работы с каталогом юнитов BattleTech Alpha Strike. Приложение получает данные с FastAPI-сервера `Maskirovka_server` и предоставляет интерфейс для просмотра, фильтрации, сортировки и управления юнитами.

## Функциональность

- 🚀 **Лендинг** — презентация приложения, модули, имитация терминала и CTA
- 🔍 **Каталог юнитов** с фильтрацией и сортировкой
- 📊 **Детальная карточка юнита** с изображением, характеристиками и расчётом TMM
- ⭐ **Ангар** — локальное сохранение избранных юнитов
- 🛡️ **Управление формациями** по правилам _Battletech: Альфа удар книга командира_
- 🎲 **Трекер партии** — отслеживание брони, структуры, нагрева и критических попаданий
- 💾 **Локальное кэширование изображений** через Cache API
- 📱 **PWA** — поддержка установки на мобильное устройство и офлайн-работа
- 🎨 **Военно-терминальный интерфейс** с тёмной темой и зелёными акцентами

## Стек технологий

- **Vue 3** — современный фреймворк для построения UI
- **Vite** — сборка и dev-сервер
- **Pinia** — глобальное состояние приложения
- **Vue Router** — маршрутизация
- **vite-plugin-pwa** — PWA (service worker, precache)
- **vite-plugin-vue-devtools** — встроенная панель Vue DevTools

## Установка и запуск

### Требования

- Node.js 18+
- Установленные зависимости из `package.json`

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

Dev-сервер проксирует запросы `/api` на `http://127.0.0.1:8000` (см. `vite.config.js`).

### Сборка production

```bash
npm run build
```

Результат сборки попадёт в каталог `dist/`.

### Запуск production-сборки

```bash
npm run preview
```

### Линтер и форматирование

```bash
# Проверка ESLint
npm run lint

# Автоисправление ошибок ESLint
npm run lint:fix

# Проверка форматирования Prettier
npm run format:check

# Автоформатирование Prettier
npm run format
```

## Настройка адреса сервера

### Dev-режим

В dev-режиме клиент ходит на `/api`, который Vite проксирует на сервер. Адрес прокси можно переопределить переменной окружения:

```bash
# PowerShell
$env:VITE_API_BASE_URL="http://127.0.0.1:20256"; npm run dev
```

### Production

В production используется полный URL сервера, сохранённый в `localStorage` под ключом `ccaf_api_url`. При первом запуске приложение предложит ввести адрес на экране `/setup`.

## Публикация на GitHub Pages

Приложение настроено для автоматического деплоя на GitHub Pages через GitHub Actions.

1. Убедитесь, что репозиторий опубликован на GitHub.
2. В настройках репозитория (**Settings → Pages → Build and deployment**) выберите источник **GitHub Actions**.
3. При пуше в ветку `master` запускается workflow `.github/workflows/deploy.yml`, который собирает проект и публикует содержимое `dist/`.
4. После успешного деплоя приложение будет доступно по адресу:

```
https://maskirovkabt.github.io/ccaf/
```

> **Важно:** в `vite.config.js` установлен `base: '/ccaf/'`, чтобы статика корректно загружалась по пути репозитория. Если имя репозитория изменится, обновите и `base`.

## Структура проекта

```
CCAF (Vue Client)/
├── index.html                  # HTML-шаблон
├── package.json                # Зависимости и скрипты
├── vite.config.js              # Конфигурация Vite и PWA
├── LICENSE                     # Лицензия MIT
├── public/                     # Статические ресурсы
│   ├── icon-192x192.png
│   ├── icon-512x512.png
│   ├── favicon.ico
│   ├── logo.png
│   ├── manifest.json
│   └── fonts/                  # Шрифты Rajdhani + Share Tech Mono
└── src/
    ├── main.js                 # Точка входа
    ├── App.vue                 # Корневой layout
    ├── assets/
    │   └── base.css            # CSS-переменные, scanlines
    ├── components/             # Переиспользуемые компоненты
    ├── data/                   # Справочники критов, формаций, ролей
    ├── router/                 # Vue Router
    ├── stores/                 # Pinia-store
    ├── utils/                  # Утилиты (кэш изображений)
    └── views/                  # Экраны приложения
        ├── LandingView.vue     # Лендинг
        ├── SearchView.vue      # Каталог юнитов
        ├── MechView.vue        # Детальная карточка юнита
        ├── FormationView.vue   # Управление формациями
        └── DataView.vue        # Трекер партии
```

## Маршруты

| Путь        | Назначение                         |
| ----------- | ---------------------------------- |
| `/`         | Лендинг приложения                 |
| `/setup`    | Первый запуск: ввод адреса сервера |
| `/search`   | Каталог юнитов с фильтрами         |
| `/mech/:id` | Детальная карточка юнита           |
| `/force`    | Управление боевыми формациями      |
| `/data`     | Трекер текущей партии              |

## Хранение данных

Приложение использует `localStorage` для локального хранения:

- `ccaf_api_url` — адрес сервера
- `ccaf_hangar` — список ID юнитов в ангаре
- `ccaf_formations` — созданные формации
- `ccaf_active_game` — активная партия в трекере

## Лицензия

Исходный код проекта распространяется под лицензией [MIT](LICENSE).

---

_Создано с ❤️ для сообщества российской лиги BattleTech Alpha Strike_
